import { AppStateInterface } from "./types";

const defaults: AppStateInterface = {
    hydration: 60,
    flourWeight: 500,
    waterWeight: 300,
    totalDoughWeight: 800,
};

export default defaults;
