import { Preset } from "../types";
import NeapolitanForm from "./Form";

const neapolitanPreset: Preset = {
    id: "neapolitan",
    label: "Neapolitan pizza",
    Form: NeapolitanForm,
};

export default neapolitanPreset;
