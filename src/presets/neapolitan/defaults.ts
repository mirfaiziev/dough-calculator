import { NeapolitanState } from "./types";

const defaults: NeapolitanState = {
    hydration: 70,
    total: 460,           // 2 servings × 230g
    ballWeight: 230,
    saltPercent: 2.5,
    poolishOn: true,
    poolishMass: 200,
    poolishHydration: 100,
};

export default defaults;
